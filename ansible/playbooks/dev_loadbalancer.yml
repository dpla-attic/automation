---

- name: Gather inventory facts in case playbook is run standalone
  hosts: all

- name: Development loadbalancer
  hosts: loadbalancer
  become: yes
  roles:
    - { role: standalone_loadbalancer, tags: ['standalone_loadbalancer', 'loadbalancer'] }
